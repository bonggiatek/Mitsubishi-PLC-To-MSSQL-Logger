<Window x:Class="PLCDataLogger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PLCDataLogger"
        xmlns:vm="clr-namespace:PLCDataLogger.ViewModels"
        mc:Ignorable="d"
        Title="PLC Data Logger" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Inverse Boolean Converter -->
        <local:InverseBooleanConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Connection Panel -->
        <Border Grid.Row="0" BorderBrush="#2196F3" BorderThickness="1" CornerRadius="5" Padding="15" Background="#F5F5F5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Row 1: Connection Settings -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="PLC IP:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PlcIpAddress, UpdateSourceTrigger=PropertyChanged}"
                         Padding="5" Margin="0,0,20,0" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Port:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding PlcPort, UpdateSourceTrigger=PropertyChanged}"
                         Padding="5" Margin="0,0,20,0" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"/>

                <StackPanel Grid.Row="0" Grid.Column="4" Orientation="Horizontal">
                    <Button Content="Connect" Command="{Binding ConnectCommand}" Width="100" Padding="10,5" Margin="0,0,10,0"
                            IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                    <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Width="100" Padding="10,5"
                            IsEnabled="{Binding IsConnected}"
                            Background="#F44336" Foreground="White" BorderThickness="0"/>
                </StackPanel>

                <!-- Row 2: Status -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Status:" VerticalAlignment="Center" Margin="0,10,10,0" FontWeight="Bold"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,10,0,0">
                    <Ellipse Width="12" Height="12" Margin="0,0,10,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="Red"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="Green"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding ConnectionStatus}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="1" BorderBrush="#2196F3" BorderThickness="1" CornerRadius="5" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Live PLC Register Data" FontSize="16" FontWeight="Bold"
                           Margin="10" Foreground="#2196F3"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Registers}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                          AlternatingRowBackground="#F9F9F9" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" RowHeight="35">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Register" Binding="{Binding RegisterAddress}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#2196F3"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
                        <DataGridTextColumn Header="Last Update" Binding="{Binding LastUpdate}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Panel -->
        <Border Grid.Row="2" BorderBrush="#2196F3" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,10,0,0" Background="#F5F5F5">
            <StackPanel Orientation="Horizontal">
                <Button Content="Configure Registers" Click="ConfigureButton_Click"
                        Width="150" Padding="10,5" Margin="0,0,10,0"
                        Background="#9C27B0" Foreground="White" BorderThickness="0"/>
                <Button Content="Write Test Value (D1000 = 123)" Command="{Binding WriteTestValueCommand}"
                        Width="220" Padding="10,5" Margin="0,0,10,0"
                        IsEnabled="{Binding IsConnected}"
                        Background="#FF9800" Foreground="White" BorderThickness="0"/>
                <TextBlock Text="Use this to test writing to PLC register D1000" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
